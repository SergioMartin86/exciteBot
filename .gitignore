# === Copyrighted materials — NEVER commit ===
# Game ROM (Nintendo)
*.nes
# NES disassembly (cyneprepou4uk NES-Games-Disassembly)
*.asm
# NOTE: reference TAS movies (*.sol) are LordTom's, licensed Creative Commons
# (tasvideos.org/1348M) and redistributable — intentionally NOT ignored.

# === Extracted game level data (regenerable from the dumps via tools/extract_track_layout.py) ===
# The full 6-lane track tile array is verbatim game content -- keep it out of the repo like the ROM.
source/track_layout.hpp
# Per-section feature lists (regenerable from docs/track_sections.txt via tools/gen_sections_header.py)
source/track_sections.hpp

# === RAM dumps / state ===
*.ram
*.state
*.sav
*.srm

# === Binaries / build artifacts ===
jaffar-player
*.o
*.a
*.so
*.so.*
*.dylib
*.exe
*.bin
*.out
build/
# NES disassembly includes (cyneprepou4uk, same provenance as *.asm)
*.inc

# Python
__pycache__/
